# YAMORU プロジェクトガイド

## 正本

- 作業を始める前に`docs/index.md`を読む。
- 現在の作業に関係するプロダクト、コンポーネント、意思決定の文書だけを追加で読む。
- 現在のプロダクト計画は`docs/product/yamoru-project-plan.md`を正本とする。
- 現在の意思決定の索引は`docs/decisions/index.md`を正本とする。個別のYDRは`docs/decisions/`配下の各`type: Decision`文書を読む。

## 意思決定の扱い

- `Accepted`であり、後続の決定によって置き換えられていない判断だけを現在有効として扱う。
- `Superseded`、`Rejected`、`Deprecated`の判断は、現在の仕様ではなく過去の経緯として扱う。
- `Accepted`の判断を黙って書き換えない。変更が必要な場合は新しいYDRを提案し、置き換え関係を明記する。
- 文書間に矛盾があり、明示的な置き換え関係でも解決できない場合は、作業を止めて矛盾を報告する。

## 作業上の約束

- 一つの変更は、一つの目的に集中させる。
- 家庭の実データ、非公開資料へのリンク、認証情報、秘密情報をコミットしない。
- 利用者による無関係な変更を保持する。
- 実際に行った確認だけを報告し、未実施の確認を成功したと表現しない。

## 作業フロー

個人リポジトリでの試作的な開発(バイブコーディング)のため、次を標準の進め方とする。

- issueに着手する指示を受けたら、実装・自動テスト・品質チェック・commit・push・PR作成まで、途中で承認を求めず進めてよい。
- PRを作成したら、確認を挟まずCI・レビューコメントの監視を開始する(PR監視の仕組みがあれば使う)。CIが赤の場合やレビュー対応が必要な場合は、通常のPR運用ルール(CI red対応、レビュー対応の手順)に従って自律的に対応を続ける。
- PRを実際にマージする操作だけは、明示の指示があるまで実行せず確認を求める。
